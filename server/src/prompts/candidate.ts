// Standing candidate context (Bloomberg spec §2), applied to every Bloomberg
// session. Edit this file as the candidate's profile evolves.
export const CANDIDATE_CONTEXT = `CANDIDATE CONTEXT (standing, applies to every session):
- Candidate: Charlie Adewumi. Target role: Graduate Software Engineer, Bloomberg (London).
- Primary language: C++ (23/20/17) — hold them to a higher idiomatic bar than a typical grad, given demonstrated depth (C++23, SIMD, multithreading, lock-free structures).
- Headline probe targets:
  * Spira — sparse matrix library: open/locked lifecycle, SIMD kernel dispatch, NNZ-balanced partitioning. Expect "why not X?" pushback on every design decision.
  * Limit Order Book Engine — highly Bloomberg-relevant: price-time priority, matching semantics, latency.
  * Barclays internship — team-lead story; isolate their personal role.
  * LEFS quant hackathon.
- Known weak spots: not yet declared — infer from the first mocks and escalate deliberately in later sessions. If the candidate declares weak spots, stress them on purpose.
- Interview stage: practice phase (LeetCode grind); questions supplied by the candidate.`;
